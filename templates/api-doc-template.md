# Шаблон: API документация

Используйте этот шаблон для документирования REST API вашего проекта.

---

# [Название API] — Документация

## Обзор

**[Название API]** предоставляет [описание функциональности].

## Базовая информация

- **Базовый URL:** `https://api.example.com/v1`
- **Аутентификация:** [тип аутентификации: API Key, OAuth, JWT и т.д.]
- **Формат ответа:** JSON
- **Лимит запросов:** [количество запросов в час]

## Аутентификация

[Описание как аутентифицироваться]

**Пример заголовка:**

```
Authorization: Bearer YOUR_API_KEY
```

## Эндпоинты

### 1. [Название эндпоинта]

**[METHOD]** `[PATH]`

[Описание]

**Параметры запроса:**

| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|---------|
| [param1] | [type] | Да/Нет | [Описание] |

**Пример запроса:**

```bash
curl -X [METHOD] https://api.example.com/v1[PATH] \
  -H "Authorization: Bearer your_api_key" \
  -H "Content-Type: application/json" \
  -d '{ "key": "value" }'
```

**Пример ответа (200 OK):**

```json
{
  "id": "...",
  "status": "success",
  "data": { ... }
}
```

**Коды ошибок:**
- `400` — Некорректный запрос
- `401` — Не авторизованы
- `404` — Ресурс не найден
- `500` — Ошибка сервера

### 2. [Еще один эндпоинт]

[Аналогичная структура]

## Глобальные коды ошибок

| Код | Описание |
|-----|---------|
| `400` | Некорректный запрос |
| `401` | Не авторизованы |
| `429` | Превышен лимит запросов |
| `500` | Ошибка сервера |

## Примеры на [язык программирования]

```python
# Пример на Python
import requests

API_KEY = "your_api_key"
response = requests.get(
    "https://api.example.com/v1/endpoint",
    headers={"Authorization": f"Bearer {API_KEY}"}
)
print(response.json())
```

## Поддержка

- **Email:** api-support@example.com
- **Документация:** https://docs.example.com
- **Статус сервера:** https://status.example.com
